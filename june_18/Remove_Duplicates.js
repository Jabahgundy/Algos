//2. remove duplicates
const array = [1, 2, 3, 5, 1, 5, 9, 1, 2, 8];
const remover_D = function (array) {
    if (array.length === 0) {
        return 0;

    }

    let pointer1 = 1
    for (let pointer2 = 1; pointer2 < array.length;
        pointer2++) {
        if ()
    }
}
